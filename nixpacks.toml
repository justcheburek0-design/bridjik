[phases.setup]
nixPkgs = ["...", "ffmpeg"]

[phases.install]
cmds = [
    "python -m venv --copies /opt/venv",
    ". /opt/venv/bin/activate",
    "pip install --upgrade build setuptools"
]

[phases.build]
cmds = [
    ". /opt/venv/bin/activate",
    "pip install ."
]