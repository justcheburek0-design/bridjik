[
  {
    "type": "function",
    "function": {
      "name": "get_player_info",
      "description": "Get player information from MineBridge API by nickname or Telegram ID",
      "parameters": {
        "type": "object",
        "properties": {
          "query": {
            "type": "string",
            "description": "Player nickname or Telegram ID"
          }
        },
        "required": ["query"]
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "get_stickers",
      "description": "Get list of available stickers. You can use sticker: [sticker:name] (max 1 sticker for message)",
      "parameters": {
        "type": "object",
        "properties": {},
        "required": []
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "get_news",
      "description": "Get latest news from MineBridge news feed",
      "parameters": {
        "type": "object",
        "properties": {
          "limit": {
            "type": "integer",
            "description": "Number of news items to fetch (max: 20)",
            "default": 10
          },
          "offset": {
            "type": "integer",
            "description": "Offset for pagination",
            "default": 0
          }
        },
        "required": []
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "get_top_players",
      "description": "Get top players from MineBridge",
      "parameters": {
        "type": "object",
        "properties": {
          "limit": {
            "type": "integer",
            "description": "Number of players to fetch (max: 15)",
            "default": 5
          },
          "offset": {
            "type": "integer",
            "description": "Offset for pagination",
            "default": 0
          }
        },
        "required": []
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "web_search",
      "description": "Search the web for current information, news, or any topic. Use this when you need up-to-date information or facts not in your knowledge base.",
      "parameters": {
        "type": "object",
        "properties": {
          "query": {
            "type": "string",
            "description": "Search query"
          },
          "max_results": {
            "type": "integer",
            "description": "Maximum number of results to return (max: 10)",
            "default": 5
          }
        },
        "required": ["query"]
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "add_sticker",
      "description": "Add a new sticker to the database. Analyze the sticker's visual content and generate a brief Russian description (2-4 words) of what is depicted or the emotion conveyed. Use this when user asks to add/save/remember a sticker.",
      "parameters": {
        "type": "object",
        "properties": {
          "description": {
            "type": "string",
            "description": "Brief Russian description of the sticker (2-4 words), e.g. 'грустный кот', 'пепе смеется', 'лайк'"
          },
          "message_id": {
            "type": "integer",
            "description": "Telegram message ID of the sticker to add"
          }
        },
        "required": ["description", "message_id"]
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "rename_sticker",
      "description": "Rename an existing sticker in the database. Use this when the user asks to rename or change the name of a sticker.",
      "parameters": {
        "type": "object",
        "properties": {
          "old_name": {
            "type": "string",
            "description": "Current name of the sticker to rename (optional if message_id is provided)"
          },
          "new_name": {
            "type": "string",
            "description": "New name for the sticker - a brief Russian description (2-4 words)"
          },
          "message_id": {
            "type": "integer",
            "description": "Telegram message ID of the sticker (optional if old_name is not provided)"
          }
        },
        "required": ["new_name"]
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "set_guess",
      "description": "Set the guessed object for the 'Who am I?' game. Use this ONCE at the start of the game to save what you've chosen. The object will be saved and you'll see it in future messages.",
      "parameters": {
        "type": "object",
        "properties": {
          "object": {
            "type": "string",
            "description": "The Minecraft object you've chosen (mob, item, creature, GUI interface, event like rain, etc.) in Russian"
          }
        },
        "required": ["object"]
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "clear_guess",
      "description": "Clear the guessed object and end the 'Who am I?' game. Use this when the user has correctly guessed the object or wants to stop the game.",
      "parameters": {
        "type": "object",
        "properties": {},
        "required": []
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "save_memory",
      "description": "Save an important moment to chat memory. Use this when you need to remember a fact about a person, event, agreement, or other important information. Records should be informative and contain enough context.",
      "parameters": {
        "type": "object",
        "properties": {
          "content": {
            "type": "string",
            "description": "Detailed description of what to remember (preferably more details)"
          },
          "category": {
            "type": "string",
            "enum": ["user_facts", "events", "agreements", "games", "other"],
            "description": "Memory category: user_facts (facts about people), events (events), agreements (agreements), games (games), other (other)"
          },
          "tags": {
            "type": "array",
            "items": { "type": "string" },
            "description": "Tags for search (people names, keywords)"
          }
        },
        "required": ["content", "category"]
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "delete_memory",
      "description": "Delete a memory record by user request. Used when information is outdated or user asks to forget something.",
      "parameters": {
        "type": "object",
        "properties": {
          "search_query": {
            "type": "string",
            "description": "Search query to find the record to delete"
          }
        },
        "required": ["search_query"]
      }
    }
  }
]
