[
  {
    "type": "function",
    "function": {
      "name": "get_stickers",
      "description": "Get list of available stickers.",
      "parameters": {
        "type": "object",
        "properties": {},
        "required": []
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "get_news",
      "description": "Get latest news from MineBridge news feed",
      "parameters": {
        "type": "object",
        "properties": {
          "limit": {
            "type": "integer",
            "description": "Number of news items to fetch (max: 20)",
            "default": 10
          },
          "offset": {
            "type": "integer",
            "description": "Offset for pagination",
            "default": 0
          }
        },
        "required": []
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "web_search",
      "description": "Search the web for current information, news, or any topic. Use this when you need up-to-date information or facts not in your knowledge base.",
      "parameters": {
        "type": "object",
        "properties": {
          "query": {
            "type": "string",
            "description": "Search query"
          },
          "max_results": {
            "type": "integer",
            "description": "Maximum number of results to return (max: 10)",
            "default": 5
          }
        },
        "required": ["query"]
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "add_sticker",
      "description": "Add a new sticker to the database. Analyze the sticker's visual content and generate a brief Russian description (2-4 words) of what is depicted or the emotion conveyed. Use this when user asks to add/save/remember a sticker.",
      "parameters": {
        "type": "object",
        "properties": {
          "description": {
            "type": "string",
            "description": "Brief Russian description of the sticker (2-4 words), e.g. 'грустный кот', 'пепе смеется', 'лайк'"
          },
          "message_id": {
            "type": "integer",
            "description": "Telegram message ID of the sticker to add"
          }
        },
        "required": ["description", "message_id"]
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "rename_sticker",
      "description": "Rename an existing sticker in the database. Use this when the user asks to rename or change the name of a sticker.",
      "parameters": {
        "type": "object",
        "properties": {
          "old_name": {
            "type": "string",
            "description": "Current name of the sticker to rename (optional if message_id is provided)"
          },
          "new_name": {
            "type": "string",
            "description": "New name for the sticker - a brief Russian description (2-4 words)"
          },
          "message_id": {
            "type": "integer",
            "description": "Telegram message ID of the sticker (optional if old_name is not provided)"
          }
        },
        "required": ["new_name"]
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "set_guess",
      "description": "Set the guessed object for the 'Who am I?' game. Use this ONCE at the start of the game to save what you've chosen. The object will be saved and you'll see it in future messages.",
      "parameters": {
        "type": "object",
        "properties": {
          "object": {
            "type": "string",
            "description": "The object you've chosen in Russian"
          }
        },
        "required": ["object"]
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "clear_guess",
      "description": "Clear the guessed object and end the 'Who am I?' game. Use this when the user has correctly guessed the object or wants to stop the game.",
      "parameters": {
        "type": "object",
        "properties": {},
        "required": []
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "save_memory",
      "description": "Save important information to memory.",
      "parameters": {
        "type": "object",
        "properties": {
          "scope": {
            "type": "string",
            "enum": ["chat", "user"],
            "description": "Memory type: 'chat' for local memes/jokes/chat events, 'user' for global personal info"
          },
          "content": {
            "type": "string",
            "description": "What to remember in NEUTRAL TONE"
          },
          "tags": {
            "type": "array",
            "items": { "type": "string" },
            "description": "Tags for search (names, keywords, dates)"
          }
        },
        "required": ["scope", "content"]
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "update_memory",
      "description": "Update existing memory record. Use when user wants to edit or correct previously saved information.",
      "parameters": {
        "type": "object",
        "properties": {
          "search_query": {
            "type": "string",
            "description": "Search query to find the record to update"
          },
          "scope": {
            "type": "string",
            "enum": ["chat", "user"],
            "description": "Where to search: 'chat' or 'user' memory (optional, searches in current chat by default)"
          },
          "content": {
            "type": "string",
            "description": "New content (optional, leave empty to keep current)"
          },
          "tags": {
            "type": "array",
            "items": { "type": "string" },
            "description": "New tags (optional, leave empty to keep current)"
          }
        },
        "required": ["search_query"]
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "delete_memory",
      "description": "Delete a memory record. Used when information is outdated or user asks to forget something.",
      "parameters": {
        "type": "object",
        "properties": {
          "search_query": {
            "type": "string",
            "description": "Search query to find the record to delete"
          },
          "scope": {
            "type": "string",
            "enum": ["chat", "user"],
            "description": "Where to search: 'chat' or 'user' memory (optional, searches in current chat by default)"
          }
        },
        "required": ["search_query"]
      }
    }
  }
]
