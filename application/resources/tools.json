[
  {
    "type": "function",
    "function": {
      "name": "get_player_info",
      "description": "Get player information from MineBridge API by nickname or Telegram ID",
      "parameters": {
        "type": "object",
        "properties": {
          "query": {
            "type": "string",
            "description": "Player nickname or Telegram ID"
          }
        },
        "required": ["query"]
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "set_reaction",
      "description": "Put a reaction (emoji) on a specific message. Use this when you want to express emotion about a user's message (e.g., set '👍' or '🔥').",
      "parameters": {
        "type": "object",
        "properties": {
          "emoji": {
            "type": "string",
            "description": "Emoji to set, e.g. '👍', '🔥', '❤️', '😱'"
          },
          "message_id": {
            "type": "integer",
            "description": "Message ID to react to"
          }
        },
        "required": ["emoji", "message_id"]
      }
    }
  },

  {
    "type": "function",
    "function": {
      "name": "get_stickers",
      "description": "Get list of available stickers. You can sometimes use sticker: [sticker:name]",
      "parameters": {
        "type": "object",
        "properties": {},
        "required": []
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "get_news",
      "description": "Get latest news from MineBridge news feed",
      "parameters": {
        "type": "object",
        "properties": {
          "limit": {
            "type": "integer",
            "description": "Number of news items to fetch (max: 20)",
            "default": 10
          },
          "offset": {
            "type": "integer",
            "description": "Offset for pagination",
            "default": 0
          }
        },
        "required": []
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "get_top_players",
      "description": "Get top players from MineBridge",
      "parameters": {
        "type": "object",
        "properties": {
          "limit": {
            "type": "integer",
            "description": "Number of players to fetch (max: 15)",
            "default": 5
          },
          "offset": {
            "type": "integer",
            "description": "Offset for pagination",
            "default": 0
          }
        },
        "required": []
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "web_search",
      "description": "Search the web for current information, news, or any topic. Use this when you need up-to-date information or facts not in your knowledge base.",
      "parameters": {
        "type": "object",
        "properties": {
          "query": {
            "type": "string",
            "description": "Search query"
          },
          "max_results": {
            "type": "integer",
            "description": "Maximum number of results to return (max: 10)",
            "default": 5
          }
        },
        "required": ["query"]
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "add_sticker",
      "description": "Add a new sticker to the database. Analyze the sticker's visual content and generate a brief Russian description (2-4 words) of what is depicted or the emotion conveyed. Use this when user asks to add/save/remember a sticker.",
      "parameters": {
        "type": "object",
        "properties": {
          "description": {
            "type": "string",
            "description": "Brief Russian description of the sticker (2-4 words), e.g. 'грустный кот', 'пепе смеется', 'лайк'"
          },
          "message_id": {
            "type": "integer",
            "description": "Telegram message ID of the sticker to add"
          }
        },
        "required": ["description", "message_id"]
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "rename_sticker",
      "description": "Переименовать существующий стикер в базе данных. Используй когда пользователь просит переименовать или изменить название стикера.",
      "parameters": {
        "type": "object",
        "properties": {
          "old_name": {
            "type": "string",
            "description": "Текущее название стикера для переименования (опционально, если указан message_id)"
          },
          "new_name": {
            "type": "string",
            "description": "Новое название стикера - краткое русское описание (2-4 слова), e.g. 'грустный кот', 'пепе смеется'"
          },
          "message_id": {
            "type": "integer",
            "description": "ID сообщения со стикером (опционально, если old_name не указан)"
          }
        },
        "required": ["new_name"]
      }
    }
  }
]
